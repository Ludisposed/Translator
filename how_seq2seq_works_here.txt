Sorry Using Chinese Here

Ok

这里对模型循环了3000次
主要的一段运行代码在最后倒数第二个模块

这个模型中需要重置，反复运行这段训练代码的话会保存之前的训练结果

sess.run(tf.global_variables_initializer())

总的来说就是输入值->编码输入值（embedding）-> 然后我就不知道了。。。。（某些关于如何解码的操作吧）

关于这里的三个loop
是在解码的过程中的
第一个loop_fn 我大胆猜测第一个if是指。。。。第一次预测之前没有预测值（也就是第一个loop函数）
辣么else的返回值就是第二个loop函数

接下来一切解码的乱七八糟的操作全部基于这个tf.nn.raw_rnn的返回值进行的

讲真这个神奇的tf.nn.bidirectional_dynamic_rnn我并没有找到哪里用了啊

关于权重和偏移的使用这块已经完全忘记了，算法上应该是逻辑算法，因为只有一个返回值
就酱紫吧
